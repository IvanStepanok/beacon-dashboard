# Baked into the client bundle at build time (NEXT_PUBLIC_* = public, not a secret).
# Without this, lib/api.ts falls back to http://localhost:8080 and every API call
# from the deployed dashboard silently dies ("Invalid email or password" on login).
NEXT_PUBLIC_BEACON_API=https://beacon-api.stepanok.com
